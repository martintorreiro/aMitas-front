const MyProfile = ({user}) => {
    
    const sharedList = "hols"

    return(
        <section>
            <ul>
                {user}
            </ul>

        </section>
    )
}

export default MyProfile;